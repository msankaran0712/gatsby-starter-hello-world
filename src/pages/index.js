import * as React from 'react'
import { CustomCheckbox } from '../CustomCheckbox'

const IndexPage = () => {
  return (
    <div align="center" style={{ padding: 80 }}>
      Modus React Checkbox:
      <CustomCheckbox></CustomCheckbox>
    </div>
  )
}

export default IndexPage
